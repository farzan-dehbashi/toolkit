# Django 2

Django web framework examples — Vidly movie app.

## Setup
```bash
pipenv install
pipenv shell
python manage.py migrate
python manage.py runserver
```

## Structure
- `vidly2/` — main app (models, views, urls)
- `manage.py` — Django management script
- `Pipfile` — dependencies
